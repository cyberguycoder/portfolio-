
// Small interactive touches
document.addEventListener('DOMContentLoaded', function(){
  // Nothing heavy — placeholder for future interactivity
  console.log('Sumit Portfolio loaded');
});
  console.log(prompt:message)  
  addEventListener.apply(requestAnimationFrame);

  // here we have generate the animated frame with the message 
  console.log('message has been send')
     